{
  "name": "nhhs-idioms-cloudflare-supabase",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/apply-unit1.mjs && node scripts/apply-unit2.mjs && node scripts/check-public.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:load": "node scripts/load-test-worker.mjs",
    "test:live-load": "node scripts/live-load-test.mjs",
    "cf:dev": "wrangler dev",
    "cf:check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "4.118.0"
  },
  "engines": {
    "node": ">=20"
  }
}
